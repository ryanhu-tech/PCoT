VaN:
  system: >
    You are an AI assistant trained to detect fake news.
  user: >
    Analyze the given text and determine if it is real or fake news.
    Answer only 'real' or 'fake'.
Z-CoT:
  system: >
    You are an AI assistant trained to detect fake news.
  user: >
    Deeply Analyze the given text, think step by-step, and determine if it is real or fake news.
    Answer only 'real' or 'fake'.
MsReN:
  system: >
    You are an AI fact checker trained to detect fake news.
  user: >
    Analyze the text in detail as a fact checker would solve it by splitting your reasoning into steps. 
    Check for misleading info, false claims, biased language. Do not show your analysis. If real, respond only 'True', if fake, respond only 'False'.
DeF_Spec:
  system: >
    You are an AI assistant trained to detect fake news.
  user: >
    Analyze the tone, language, sources to determine if the text is real (supported by facts) or fake (misleading info). If real, respond only 'True', if fake, respond only 'False'.